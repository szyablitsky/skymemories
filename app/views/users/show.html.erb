<% provide(:title, "Пользователь #{@user.name}") %>
<h1>Пользователь</h1>
<p>
  <%= gravatar_image_tag(@user.email, alt: @user.name, class: 'gravatar') %>
  <%= "#{escape_once(@user.name)}, #{@user.email}" %>
</p>
