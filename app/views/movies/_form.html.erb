<%= form_for(@movie) do |f| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @movie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :vimeo_id, "Vimeo ID" %><br>
    <%= f.number_field :vimeo_id %>
  </div>
  <div class="field">
    <%= f.label :locale, "Язык" %><br>
    <%= f.text_field :locale %>
  </div>
  <div class="field">
    <%= f.label :main, "Основной" %><br>
    <%= f.check_box :main %>
  </div>
  <div class="actions">
    <%= f.submit "Сохранить", class: "btn btn-primary" %>
  </div>
<% end %>
